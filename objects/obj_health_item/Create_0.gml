///@desc Init

event_inherited();

my_item = 100; //100 is for the health pickup
healing = 5;

light_pulse_offset = random(2*pi);
light_pulse_divider = irandom_range(200, 300);
light_pulse_size = random_range(0.05, 0.12);